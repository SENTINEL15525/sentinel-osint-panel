json
{
  "version": 2,
  "builds": [
    {
      "src": "vercel_entrypoint.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "vercel_entrypoint.py" }
  ]
}
