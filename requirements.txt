
fastapi==0.115.0
uvicorn[standard]==0.30.6


vercel.json (exact content):
json
{
  "version": 2,
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "main.py" }
  ]
}

